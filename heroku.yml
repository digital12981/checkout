build:
  languages:
    - nodejs
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false

setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE

run:
  web: npm start

release:
  image: web
  command:
    - npm run db:push